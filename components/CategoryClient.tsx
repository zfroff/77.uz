"use client";

export default function CategoryClient({ slug }: { slug: string }) {
  return (
    <div>
      <p>This is client-side logic for the category: {slug}</p>
      {/* Add client-side logic here */}
    </div>
  );
}
